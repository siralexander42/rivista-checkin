/**
 * Page Blocks UI Helper Functions
 * Funzioni di supporto per l'interfaccia dell'editor pagine figlie
 */

// Initialize Lucide icons
lucide.createIcons();

// Initialize sidebar
document.addEventListener('DOMContentLoaded', () => {
    initSidebar('magazines');
});
